// Projects page — header turant white + sticky
document.addEventListener("DOMContentLoaded", () => {
  const header = document.getElementById("header");
  header.classList.add("header-projects");
});